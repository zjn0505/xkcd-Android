<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:layout_margin="2dp"
    android:background="?android:attr/colorBackground"
    android:padding="1dp">

    <ImageView
        android:id="@+id/itemWhatIfImageView"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerVertical="true"
        android:scaleType="fitCenter"
        android:background="#ebebeb"
        tools:src="@mipmap/ic_launcher" />

    <ImageView
        android:id="@+id/itemWhatIfFav"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="10dp"
        android:scaleType="fitCenter"
        android:visibility="gone"
        app:srcCompat="@drawable/ic_heart_on" />

    <TextView
        android:id="@+id/itemWhatIfTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_margin="1dp"
        android:layout_toEndOf="@id/itemWhatIfImageView"
        android:layout_toLeftOf="@id/itemWhatIfFav"
        android:layout_toRightOf="@id/itemWhatIfImageView"
        android:layout_toStartOf="@id/itemWhatIfFav"
        android:fontFamily="@font/xkcd_script"
        android:gravity="center_vertical"
        android:paddingLeft="10dp"
        android:paddingStart="10dp"
        android:paddingEnd="0dp"
        android:paddingRight="0dp"
        android:textSize="22sp"
        tools:text="Electrofishing for Whales" />

</RelativeLayout>
